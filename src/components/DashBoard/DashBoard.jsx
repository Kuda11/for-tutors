import React from "react";
import styles from "./DashBoard.module.scss";
import DashNavBar from "./DashNavBar"

const DashBoard = () => {
    return (
        <>
        <DashNavBar/>
        Task: <br/>
        1. Should have timer which shows when next lesson starts. <br/>
        2. Should have button which links to Live video when lesson is ready. <br/>
        3. Submit feedback of how tutor is performing for the week? <br/>
        4. 
        </>
    );
};

export default DashBoard;